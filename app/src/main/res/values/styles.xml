<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/frostbar_color</item>
        <item name="android:colorPrimaryDark">@color/frostbar_color_dark</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:colorAccent">@color/frostbar_color</item>
    </style>

    <style name="DarkTheme" parent="android:Theme.Material">

    </style>

</resources>
